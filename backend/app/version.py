# Single source of truth for the version shown in the web app's topbar and
# login screen (see frontend/js/app.js) — bump this alongside the README's
# version table and the git tag on every release, so a viewer can always
# tell at a glance whether they're actually looking at the latest deploy.
APP_VERSION = "0.4.1"
